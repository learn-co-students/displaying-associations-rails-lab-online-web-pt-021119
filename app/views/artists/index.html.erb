<h1><%= pluralize(@count, 'song') %></h1>
<ul>
  <% @artists.each do |a| %>
  <li><%= link_to a.name, artist_path(a) %></li>

    <ul>
        <li><%= pluralize(a.songs.count, 'song') %></li>
    </ul>

    <% end %>

</ul>